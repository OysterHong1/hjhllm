export type SelectedImage = {
  file: File;
  previewUrl: string;
};

export type SelectedAudio = {
  file: File;
  previewUrl: string;
  durationMs: number;
};

export type SelectedVideo = {
  file: File;
  previewUrl: string;
};
